import ClaimsPortal from '@/components/claims/ClaimsPortal';

export const metadata = {
  title: 'Claim $MT Rewards | MT-ECO SYSTEM',
  description: 'Claim your MemeTorrent $MT rewards — daily check-ins and community rewards sent to your linked Solana wallet.',
};

export default function ClaimsPage() {
  return <ClaimsPortal />;
}